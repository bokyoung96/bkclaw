# datas (legacy)

이 디렉토리는 legacy 경로입니다.

새 파일은 가능하면 아래로 이동합니다:
- durable dataset -> `data/`
- run artifact -> `outputs/`
- human-facing summary -> `reports/`

현재 legacy 예시였던 quant DB 요약 문서는 `reports/legacy/`로 이동했습니다.
